<Window x:Class="RevitToGISsupport.UI.BrowserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="BIM Sync - Control Panel" Height="550" Width="380"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI, Tahoma, sans-serif"
        Background="#F3F2F1"
        ResizeMode="NoResize">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#323130"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#8A8886"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#323130"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="White" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsKeyboardFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#0078D4"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#106EBE"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#005A9E"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#C8C6C4"/>
                    <Setter Property="Foreground" Value="#605E5C"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#323130"/>
            <Setter Property="BorderBrush" Value="#8A8886"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F3F2F1"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#EDEBE9"/>
                </Trigger>
            </Style.Triggers>
        </Style>

    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" Padding="25,20" BorderBrush="#EDEBE9" BorderThickness="0,0,0,1">
            <StackPanel>
                <TextBlock Text="BIM Sync Agent" FontSize="22" FontWeight="Bold" Foreground="#0078D4"/>
                <TextBlock Text="Trạm kết nối dữ liệu giữa Revit và Web" FontSize="13" Foreground="#605E5C" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="1" Margin="25,20">
            <TextBlock Text="CHỌN FILE REVIT ĐỂ ĐỒNG BỘ" FontWeight="SemiBold" FontSize="11" Foreground="#605E5C" Margin="0,0,0,6"/>
            <ComboBox x:Name="cbDocuments" Height="38" Margin="0,0,0,15" FontSize="14" Padding="10,0" VerticalContentAlignment="Center" SelectionChanged="cbDocuments_SelectionChanged"/>

            <TextBlock Text="TÊN NGƯỜI DÙNG" FontWeight="SemiBold" FontSize="11" Foreground="#605E5C" Margin="0,0,0,6"/>
            <TextBox x:Name="tbUserName" Height="38"/>

            <TextBlock Text="MÃ DỰ ÁN (PROJECT ID)" FontWeight="SemiBold" FontSize="11" Foreground="#605E5C" Margin="0,0,0,6"/>
            <TextBox x:Name="tbProjectId" Height="38" Text="P001" Margin="0,0,0,20" TextChanged="tbProjectId_TextChanged"/>

            <StackPanel x:Name="panelShare" Visibility="Collapsed" Margin="0,0,0,25">
                <TextBlock Text="LINK TRUY CẬP WEB (GỬI CHO NGƯỜI KHÁC)" FontWeight="SemiBold" FontSize="11" Foreground="#605E5C" Margin="0,0,0,6"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="tbShareLink" Grid.Column="0" Height="38" IsReadOnly="True" Background="#F3F2F1" Foreground="#0078D4" FontWeight="SemiBold"/>
                    <Button x:Name="btnCopy" Grid.Column="1" Width="70" Margin="8,0,0,0" Content="Copy" Style="{StaticResource SecondaryButton}" Click="btnCopy_Click"/>
                </Grid>
            </StackPanel>

            <Grid Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button x:Name="btnPull" 
                        Grid.Column="0" 
                        Content="TẢI LỆNH VỀ" 
                        Style="{StaticResource SecondaryButton}" 
                        Height="45" 
                        Click="btnPull_Click"/>

                <Button x:Name="btnPublish" 
                        Grid.Column="2" 
                        Style="{StaticResource PrimaryButton}" 
                        Height="45" 
                        Content="ĐỒNG BỘ LÊN" 
                        Click="btnPublish_Click"/>
            </Grid>
        </StackPanel>

        <Border Grid.Row="2" Background="White" BorderBrush="#EDEBE9" BorderThickness="0,1,0,0" Padding="15,10">
            <DockPanel>
                <TextBlock Text="Status:" Foreground="#605E5C" Margin="0,0,5,0" FontWeight="SemiBold"/>
                <TextBlock x:Name="lblStatus" Text="Ready." Foreground="#28A745" FontWeight="SemiBold"/>
            </DockPanel>
        </Border>
    </Grid>
</Window>